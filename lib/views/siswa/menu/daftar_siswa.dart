import 'package:flutter/material.dart';

class DaftarKelasSiswaPage extends StatelessWidget {
  const DaftarKelasSiswaPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Daftar Siswa'),
      ),
    );
  }
}
